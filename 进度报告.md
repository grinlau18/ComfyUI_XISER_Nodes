# 进度报告（当前快照）

## 概览
- 已在 v3 加载的模块：logic, ui_control, list_processing, data_processing, label, set_color, image_and_mask, canvas（含 XIS_Canvas / XIS_CanvasMaskProcessor）。
- 待迁移 A 级模块：resize_image_or_mask, psd_layer_extract, adjust_image, image_puzzle, shape_data, sampling。
- 待迁移 S 级模块：curve_editor, coordinate_path, reorder_images, shape_and_text, canvas_mask_processor, multi_point_gradient, llm.orchestrator, image_manager.node。
- 顶层入口已切换到 v3-only，legacy 映射不再加载；前端沿用 Vue/传统双界面。

## 最新进展
- 完成 XIS_Canvas 与 XIS_CanvasMaskProcessor 的 v3 + Vue 适配，画布事件与尺寸调整问题已修复。
- 文档整合为三份：项目计划、技术指导、进度报告，去除重复计划与清单。
- 聚合层仅加载已迁移模块，未迁移的 A/S 级模块保持禁用。

## 近期计划
- A1：完成 resize_image_or_mask 与 sampling 相关节点的 v3 迁移与用例验证。
- A2：迁移 psd_layer_extract / adjust_image（含前端）、image_puzzle、shape_data，验证 Vue/传统界面序列化。
- S 阶段准备：梳理 curve_editor 等重前端节点的事件与数据格式，制定专用测试用例。

## 风险与关注点
- 前端交互节点的 Vue 兼容性仍需逐一回归；多节点全局 wheel 捕获需注意销毁监听。
- v3 API 版本更新可能带来类型变更，需定期对齐 `comfy_api.latest`。
- 未迁移模块暂不加载，若有用户依赖需提前告知或提供 legacy 兜底方案。 
